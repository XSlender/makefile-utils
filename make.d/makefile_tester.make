_makefile-test:
	@printf "${LINE_BREAK}"
	@printf "${GREEN}Makefile is initialized ${CHECK_MARK}${RESET}"
	@printf "${LINE_RETURN}"
	@printf "${RIGHT_ARROW}\tYou can try it out using 'make help'"
	@printf "${LINE_BREAK}"
	@printf "${RIGHT_ARROW}\tYou can also modify the includes directly in the Makefile fitting your needs."
	@printf "${LINE_RETURN}"
